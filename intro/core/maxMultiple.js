function maxMultiple(divisor, bound) {
    let start = 1;
        let temp = 0;
        
        while(start <= bound){
            if(start % divisor == 0){
                temp = start
               start++
               }
            start++
              
              }
        return temp
    }
    